[theme]
primaryColor = "#E6007E"

[server]
# "poll" avoids the inotify watch limit on Streamlit Community Cloud (the default
# watchdog/inotify watcher floods the logs with "inotify watch limit reached"),
# while still supporting hot-reload locally.
fileWatcherType = "poll"
